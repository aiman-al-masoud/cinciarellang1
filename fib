# viva la ricorsività! viva i linguaggi ricorsivi!
fib = fun(n){

    if n == 0 || n ==1 {
        ret = 1;
    }else{
        ret = fib(n-1) + fib(n-2);
    }

    ret;
};

x = fib(10); # capra capretta

print(x);